{
  "name": "hzxgj-assets",
  "version": "1.0.0",
  "description": "静态资源工程",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r src/* dist/ && find dist -name '*.json' -exec python3 -c 'import json; d=json.load(open(\"{}\")); json.dump(d, open(\"{}\", \"w\"), separators=(\",\", \":\"), ensure_ascii=False)' \\;"
  }
}