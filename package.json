{
  "name": "hzxgj-assets",
  "version": "1.0.0",
  "description": "静态资源工程",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp -r src/* dist/ && find dist -name '*.ts' -delete && find dist -name '*.js' -exec terser {} -o {} --compress --mangle \\;"
  },
  "devDependencies": {
    "terser": "^5.46.0",
    "typescript": "^4.9.0"
  }
}
